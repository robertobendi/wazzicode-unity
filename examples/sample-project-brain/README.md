# Sample project brain

These files were generated by `uvibe brain --project=tests/fixtures/sample-unity-project` (a tiny URP+InputSystem fixture). They are illustrative — your real project will produce richer output (more scenes, prefabs, scripts, ScriptableObject types).

| File | What it is |
|---|---|
| `project_brain.md` | Human-readable summary |
| `project_brain.json` | Same content, structured |
| `claude_context.md` | Concise priming context for Claude |
| `conventions.md` | First-run default; **edit freely** in your real project — `uvibe brain` won't overwrite it |
| `config.json` | Safety + runtime config; defaults to `safetyMode: read_only` |
